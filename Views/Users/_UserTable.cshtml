@model List<UserListItemViewModel>

<div class="card">
    <div class="table-wrap">
        <table class="table">
            <thead>
                <tr>
                    <th>Người dùng</th>
                    <th>Email</th>
                    <th>Loại</th>
                    <th>Vai trò</th>
                    <th>Cấp trên</th>
                    <th>Ngày tạo</th>
                    <th>Dữ liệu</th>
                    <th class="text-right">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var u in Model)
                {
                    <tr>
                        <td>
                            <div class="user-cell">
                                <partial name="_Avatar" model="@u.FullName" />
                                <span>@u.FullName @if (u.IsSelf) { <span class="muted text-sm">(bạn)</span> }</span>
                            </div>
                        </td>
                        <td class="muted">@u.Email</td>
                        <td><span class="tag @u.AccountType.CssClass() plain">@u.AccountType.Label()</span></td>
                        <td>
                            @{
                                var roleCss = u.RoleName switch
                                {
                                    Roles.SuperAdmin => "tag-high",
                                    Roles.Admin => "tag-doing",
                                    Roles.Manager => "tag-medium",
                                    _ => "tag-todo"
                                };
                            }
                            <span class="tag @roleCss plain">@u.RoleLabel</span>
                        </td>
                        <td class="muted text-sm">@(u.ManagerName ?? "—")</td>
                        <td class="muted text-sm">@u.CreatedAt.ToLocalTime().ToString("dd/MM/yyyy")</td>
                        <td class="muted text-sm">@u.OwnedWorkspaceCount workspace • @u.AssignedTaskCount task</td>
                        <td class="text-right">
                            <div class="form-actions justify-end">
                                @if (u.CanEdit)
                                {
                                    <a class="btn btn-secondary btn-sm" asp-action="Edit" asp-route-id="@u.Id">Sửa</a>
                                }
                                @if (u.CanToggleAdmin)
                                {
                                    <form class="inline-form" asp-action="ChangeRole" method="post">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@u.Id" />
                                        <input type="hidden" name="role" value="@(u.IsAdmin ? Roles.User : Roles.Admin)" />
                                        <button type="submit" class="btn @(u.IsAdmin ? "btn-secondary" : "btn-primary") btn-sm">
                                            @(u.IsAdmin ? "Bỏ quyền Admin" : "Cấp quyền Admin")
                                        </button>
                                    </form>
                                }
                                @if (u.CanDelete)
                                {
                                    <form class="inline-form" asp-action="Delete" method="post"
                                          onsubmit="return confirm('Xóa người dùng @u.FullName? Các workspace họ sở hữu (@u.OwnedWorkspaceCount) và toàn bộ dữ liệu bên trong sẽ bị xóa theo.');">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@u.Id" />
                                        <button type="submit" class="btn btn-danger btn-sm">Xóa</button>
                                    </form>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
